<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movies</title>
    <!-- Add any CSS or Bootstrap links for styling -->
</head>

<body>
    <div class="container">
        <h1>Movie List</h1>
        <ul>
            {{#each movies}}
            <li>
                <h2>{{this.title}}</h2>
                <img src="{{this.image}}" alt="{{this.title}} Image">
                <a href="./movies/{{this._id}}">See more</a> <!-- Link to individual movie details page -->
            </li>
            {{/each}}
        </ul>
    </div>
</body>

</html>